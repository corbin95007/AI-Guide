/* ===================================
   内容数据汇总入口
   将所有 Tab 内容统一挂载到 CONTENT_DATA
   新增 Tab 只需在此文件添加一行
   =================================== */

var CONTENT_DATA = {
    tabs: [
        CONTENT_TAB1,
        CONTENT_TAB2,
        CONTENT_TAB3,
        CONTENT_TAB4,
        CONTENT_TAB5,
        CONTENT_TAB6
    ]
};
